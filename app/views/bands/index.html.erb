<%= render 'header' %>

<div class="good-bands">
  <h2>List of Good Bands Will Go Here</h2>

  <ul>
  <% @goodbands.each do |goodband| %>
    <li><%= link_to goodband.name, goodbands_path(goodband) %></li>
  <% end %>
  </ul>

</div>

<div class="bad-bands">
  <h2>List of Bad Bands Will Go Here</h2>

  <ul>
  <% @badbands.each do |badband| %>
    <li><%= link_to badband.name, badbands_path(badband) %></li>
  <% end %>
  </ul>

</div>

<%= render 'footer' %>
